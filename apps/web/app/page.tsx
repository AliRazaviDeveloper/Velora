import Color from "@repo/ui/color";

const Home = () => {
  return (
    <div className="flex flex-row flex-wrap justify-center items-center">
      <Color />
    </div>
  );
};

export default Home;
